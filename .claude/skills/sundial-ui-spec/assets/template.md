# UI 設計規劃：<模組>／<畫面名稱>

<!--
這是「查詢＋列表型」畫面（SKILL.md 四種體例的第一種）的骨架模板，以 09-ui-all-attendance.md
為藍本。這是最常見、最適合當起點的形狀，但不是唯一形狀——如果手上的畫面是簽核流程、多分頁複雜
表單，或範圍還沒收斂成單一畫面，先讀 SKILL.md「四種既有體例」一節，換用對應的形狀，不要硬套
這份模板。

複製這份檔案之後，把這段註解整段刪掉。
-->

## 確認狀態

<!--
★ 這一節記錄的是一件**真的發生過的事**，不是套語。新寫的文件預設是「尚未確認」。

「本文件內容已由使用者明確同意」這句話的意思是：使用者真的逐項看過並同意了。
如果那件事還沒發生，寫上去就是偽造——而下一個人會拿它當作可以直接開工的依據，
不會回頭去問。既有文件寫著「已由使用者明確同意」，是因為那些文件當時真的走過
那個過程。

新建文件時用這一段（改掉方括號內容）：

  **尚未經使用者確認。** 本文件是依使用者指示新建的初稿，[列出哪些部分是本文件
  自行判斷的]，尚未逐項經過確認。經使用者確認後再改寫本節。

使用者確認之後才換成：

  本文件內容已由使用者{明確同意／明確確認／要求記錄}，列為正式 {UI／UI、流程與
  Schema／UI、流程與資料責任} 規劃。

選哪個受詞取決於這份文件除了畫面本身，還一併定案了流程還是新的 Schema。
-->

**尚未經使用者確認。** 本文件是依使用者指示新建的初稿，[列出哪些部分是本文件自行判斷的]，尚未逐項經過確認。經使用者確認後再改寫本節。

## 導覽結構

<!--
畫面掛在選單的哪個分組、哪個位置。用 text 區塊畫出樹狀結構，並核對分組名稱與既有 UI 文件、
apps/web/src/menu/main-menu.ts 已經用過的名稱一致（即使 main-menu.ts 現在還沒有這個分組也沒
關係，先寫成未來的樣子，但名稱要跟得上既有慣例）。
-->

> 選單位置：`<分組> → <畫面名稱>`

```text
<分組>
├─ <既有同分組項目一>
├─ <既有同分組項目二>
└─ <這份畫面>
```

## 查詢條件

<!--
列出畫面上可以篩選／查詢的欄位，用表格表示每個條件的規則（必選／可選、預設值、與其他條件的
交互關係）。只講「使用者可以怎麼查」，不講後端怎麼實作查詢。
-->

| 查詢條件 | 規則 |
| -------- | ---- |
|          |      |

## 列表

<!--
列表一列代表什麼（例如「一位員工一天一列」），然後逐欄說明「顯示內容／來源」。來源可以指名到
具體的資料表與欄位（例如 `attendance_results.worked_minutes`），但不要寫該欄位的資料型態、
長度、是否可為 NULL——那些是 docs/schema/ 的責任，寫進來會製造兩份文件互相漂移的風險。
唯一例外：如果這個畫面需要一張目前 Schema 完全不存在的新表，才可以在這裡把完整欄位表寫出來，
但事後要把同一份定義回寫進對應的 docs/schema/0X-*.md。
-->

| 欄位 | 顯示內容／來源 |
| ---- | -------------- |
|      |                |

## 顯示規則

<!--
講清楚「同一天可能同時有兩種狀態」「沒有資料時顯示什麼」這類容易被下游自行假設、但其實需要
明確規定的邊界情況。每一條都應該是讀者看了列表之後會自然問出的問題的答案。
-->

-

## 資料責任

<!--
講清楚「這個數字最終該讀哪張表」，尤其是當畫面同時牽涉「結果表」（例如 attendance_results）
與「原始事實表」（例如打卡事件）時，避免下游猜錯權威來源、或誤以為某張表要負責它不該負責的
資料。也在這裡寫清楚多公司資料隔離的邊界（哪些資料要限制在目前公司 Tenant）。
-->

-

## 本輪明確延後

<!--
不是每份文件都需要這一節——只有一兩句待定事項時，直接寫成上面各節裡的行內一句話就好（例如
「XX 入口是否開放尚未定案」），不必為了套模板硬起這個標題。

真的需要獨立成節時（待定範圍夠大、會被其他文件依賴），照著下面三個要素寫：
1. 具體列出「這一輪範圍內、但還沒決定」的項目——要寫到動作層級，不要籠統寫成整個功能都沒定案
   （尤其當這個功能的某個子動作其實已經在別份文件定案時，籠統寫法會製造假衝突）。
2. 講清楚留到哪個場合、哪份文件再決定。
3. 明文禁止下游自行補猜——這句話是這一節真正的價值所在，不要省略。
-->

本輪只確認「<畫面名稱>」的<範圍>，不確認<待定範圍>：

-
-

不因本頁需要<某個具體理由>，就自行把未確認欄位補入 `<表名>`。
