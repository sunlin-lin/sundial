/**
 * 語系檔：zh-TW（§1.3、§1.8.2）。
 *
 * **本檔只有「字」，沒有任何邏輯。** 型別、查詢入口、回落規則全在 `../messages.ts`——
 * 一支語系檔要能被翻譯的人單獨打開來改，而不必先讀懂 i18next 怎麼掛上去；
 * 反過來說，改回落規則的人也不該一路捲過幾十則中文字才找得到那段程式。
 * 新增語系＝在 `locales/` 多一個檔案，本檔一行都不用動。
 *
 * ⚠️ **這裡有好幾則訊息是刻意含糊的**（§3.2）：登入失敗不分辨四種原因、跨公司存取與「不存在」
 * 逐字相同、唯一性檢查不回聲與哪一筆重複。**理由寫在各模組的 `*.errors.ts`，不在這裡。**
 * 在這一頁把訊息「寫精確一點」看起來只是潤稿，實際上是把公司名單、帳號名單或身分證
 * 變成一支可枚舉的介面。改任何一則字之前，請先讀該錯誤碼在 `*.errors.ts` 上的說明。
 *
 * **插值以 `{{name}}` 表示**（i18next 的預設語法）。哪一則訊息吃哪些變數，由 `../messages.ts`
 * 的 `MESSAGE_PARAMS` 型別宣告——那份宣告才是編譯期擋得住的那一份，這裡的 `{{...}}` 只是字。
 */

/**
 * 業務錯誤碼 → 訊息。**這份物件的 key 就是 §1.3 的錯誤碼聯集**（見 `../messages.ts`）。
 *
 * 命名一律 `<領域>.<原因>`：領域用**單數的功能分類**、原因用 kebab-case。領域與路徑段名是兩套
 * 獨立的命名空間（§1.3）——`auth.invalid-credentials` 是對的，即使路徑上沒有 `/auth/...`。
 */
export const ZH_TW_ERRORS = {
  // --- sessions/main ---------------------------------------------------------
  /**
   * ⚠️ 刻意含糊：公司代號不存在／帳號不存在／密碼錯誤／帳號不屬於這家公司，**四種一律這一句**。
   * 寫精確等於把登入端點變成公司名單與帳號名單的枚舉介面（§3.2，理由見 `sessions-main.errors.ts`）。
   */
  'auth.invalid-credentials': '公司代號、帳號或密碼錯誤',

  // --- roles/main ------------------------------------------------------------
  /** ⚠️ 不回聲是哪一筆既有角色與它重複（§3.2）。 */
  'role.code-duplicated': '角色代碼已被使用，請換一個',
  /** ⚠️ 跨公司存取與「這個 id 根本不存在」必須**逐字相同**（§3.2）。 */
  'role.not-found': '角色不存在或已被刪除',
  'role.permission-not-found': '選取的權限不存在',
  'role.permission-not-assignable': '選取的權限不可指派，請改選底下的實際權限',
  'role.system-role-protected': '系統預設角色不可修改或刪除',
  /**
   * 帶插值的一則（變數宣告見 `../messages.ts` 的 `MESSAGE_PARAMS`）。
   *
   * ⚠️ 這裡回聲數字**不違反 §3.2**：`assignedUserCount` 是本公司自己的統計，不揭露任何成員身分，
   * 而且刪除角色的人本來就看得到自己公司的成員清單。說得出數字，使用者才知道下一步要移轉幾個人；
   * 只說「仍有成員使用」的話，他得自己回到成員頁一筆筆比對。
   */
  'role.in-use': '仍有 {{assignedUserCount}} 位公司成員使用此角色，請先將成員移轉至其他角色',
  'role.last-admin-role': '這是公司最後一個具管理能力的角色，不可刪除或停用',
  'role.state-changed': '角色狀態已變更，請重新載入後再試',

  // --- employees/main --------------------------------------------------------
  /** ⚠️ 只說重複，不回聲是哪一位員工佔用了這個編號（§3.2）。 */
  'employee.code-duplicated': '員工編號已被使用，請換一個',
  /** ⚠️ 敏感識別值：只能說無法建立，**禁止**回聲與哪一筆既有資料重複（§3.2 比員工編號更嚴）。 */
  'employee.identity-number-duplicated': '此身分證字號已存在，無法建立',
  /** ⚠️ 跨公司存取與「不存在」必須逐字相同（§3.2）。 */
  'employee.not-found': '員工不存在或已被刪除',
  'employee.state-changed': '員工資料狀態已變更，請重新載入後再試',

  // --- company-users/roles ---------------------------------------------------
  /** ⚠️ 「查無此成員」與「這位成員屬於別家公司」共用的唯一出口（§3.2）。 */
  'role-assignment.company-user-not-found': '找不到指定的公司成員',
  'role-assignment.company-user-inactive': '成員帳號已停用，無法指派角色',
  /** ⚠️ 同上：別家公司的角色、已軟刪除的角色，與不存在回同一句（§3.2、§4.3）。 */
  'role-assignment.role-not-found': '找不到指定的角色',
  'role-assignment.role-inactive': '角色已停用，無法指派',
  'role-assignment.already-assigned': '這位成員已經擁有這個角色',
  'role-assignment.not-found': '找不到可撤銷的角色指派',
  'role-assignment.last-role-required': '每個帳號至少要保留一個角色，無法全部撤銷',
  'role-assignment.state-changed': '角色指派狀態已變更，請重新載入',
} as const

/**
 * 平台層訊息：envelope 頂層 `msg` 在非 `300` 路徑上會用到的那幾句。
 *
 * **與錯誤碼分開，是因為它們不是錯誤碼。** 它們不會出現在 `errors[].code`，也不進各端點的
 * 錯誤碼宣告清單（§1.8.3）——那份清單列的是**業務**錯誤，把每支端點都可能發生的系統錯誤
 * 抄進去只是噪音。分開之後，`ErrorCode` 這個型別就只裝得下真正的業務碼。
 */
export const ZH_TW_PLATFORM = {
  /** `400`。**一律一般化**：例外訊息可能含 SQL 原文或內部路徑，對使用者沒有意義、對攻擊者卻有（§3.2）。 */
  'system.unexpected-error': '系統發生錯誤，請稍後再試',
  /** `100`。body 不是合法 JSON。 */
  'request.invalid-payload': '請求格式不正確',
  /** `100`。body 是合法 JSON 但不符 schema。出錯位置只進 log，不回給呼叫端（§1.3）。 */
  'request.invalid-schema': '請求資料不符合規格',
  /** `900`。前端據此導向登入頁。 */
  'auth.session-required': '請重新登入',
  /** `901`。被拒的細節（哪一個權限碼）只進 log，不對前端揭露（§3.1.1）。 */
  'auth.permission-denied': '沒有執行此操作的權限',
} as const
