/**
 * 語系檔：zh-TW × `modules/permissions/`（§1.3、§1.8.2）。
 *
 * **本大目錄目前一則訊息都沒有，檔案仍然要在，形狀也要對。** 理由與 §1.8.3 要求
 * 「沒有業務錯誤的端點也必須宣告空清單」是同一條：省略的話，「這個模組真的沒有訊息」與
 * 「有人忘了建檔」長得一模一樣，下一個要加訊息的人得先自己判斷該不該建、建在哪、叫什麼名字
 * ——而那正是同一件事會長出兩種形狀的起點。空的形狀擺在這裡，他只要填字。
 *
 * 空子樹在型別上算出來是 `never`（見 `../../message-tree.ts`），因此它一個 key 都不貢獻，
 * **不需要為了「讓它有東西」而先發明一個沒人用的訊息**。
 *
 * 為什麼真的沒有訊息：`tree` 是查詢類端點，「查無資料」不是錯誤而是有效答案（§3.1.3）；
 * `checkAssignable` 刻意不組錯誤，由呼叫端（`roles/main`）用自己的錯誤碼組 `errors[]`
 * ——那些碼因此落在 `roles.main.errors.*`，不在這裡。
 */

export const PERMISSIONS = {
  main: {
    errors: {},
  },
} as const
