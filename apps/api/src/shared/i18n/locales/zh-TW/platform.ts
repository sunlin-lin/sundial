/**
 * 語系檔：zh-TW × 平台層（不屬於任何模組的訊息）。
 *
 * **與各模組的錯誤碼分開，是因為它們不是錯誤碼。** 這幾則不會出現在 `errors[].code`，
 * 也不進各端點的錯誤碼宣告清單（§1.8.3）——那份清單列的是**業務**錯誤，把每支端點都可能發生的
 * 系統錯誤抄進去只是噪音。分開之後，`ErrorCode` 這個型別就只裝得下真正的業務碼。
 *
 * ---
 *
 * **key 仍然是四段，但第一段是 `platform` 這個假目錄。** 其餘語系檔的四段由模組路徑機械推導，
 * 這幾則沒有模組路徑可推——它們的產生者是 HTTP 邊界層與守衛，不是任何一支 service。
 * 硬塞進某個模組（例如把 `session-required` 放進 `sessions`）會是一句謊話：它由憑證驗證器就地產出，
 * `sessions/main` 的 service 根本沒有機會參與（見 `sessions-main.errors.ts` 檔頭）。
 *
 * 第二段因此是**產生者**而不是目錄名，而且只有三種，各對應請求生命週期的一個階段：
 * - `request`：body 解析與 schema 驗證（`100`）
 * - `auth`：認證與授權守衛（`900`／`901`）
 * - `system`：未捕捉例外的統一出口（`400`）
 *
 * 三個名字都是這裡發明的，這是這套規則唯一還需要「想名字」的地方——把它關在一個檔案、
 * 一個假目錄底下，是為了讓那件事不會擴散回模組訊息上。
 */

export const PLATFORM = {
  request: {
    errors: {
      /** `100`。body 不是合法 JSON。 */
      'invalid-payload': '請求格式不正確',
      /** `100`。body 是合法 JSON 但不符 schema。出錯位置只進 log，不回給呼叫端（§1.3）。 */
      'invalid-schema': '請求資料不符合規格',
    },
  },
  auth: {
    errors: {
      /** `900`。前端據此導向登入頁。 */
      'session-required': '請重新登入',
      /** `901`。被拒的細節（哪一個權限碼）只進 log，不對前端揭露（§3.1.1）。 */
      'permission-denied': '沒有執行此操作的權限',
    },
  },
  system: {
    errors: {
      /** `400`。**一律一般化**：例外訊息可能含 SQL 原文或內部路徑，對使用者沒有意義、對攻擊者卻有（§3.2）。 */
      'unexpected-error': '系統發生錯誤，請稍後再試',
    },
  },
} as const
